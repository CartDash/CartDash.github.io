/*class Platform {
  float xPlat, yPlat, wPlat, hPlat;

  Platform(float x1, float y1, float x2) {
    xPlat = x1;
    yPlat = y1;
    wPlat = x2;
  }
  while (imgY + 50 <= yPlat + 10 && imgY >= yPlat && imgX <= wPlat && imgX >= xPlat) {
    onPlatform = true;
  }
  if (imgY + 50 <=! yPlat + 10 || imgY >=! yPlat || imgX <=! wPlat || imgX >= xPlat) {
    onPlatform = false;
  }

  void display() {
    fill(0, 255, 0);
    rect(xPlat, yPlat, wPlat, yPlat);
  }
}
*/
/*boolean rectIntersect(Sprite r1, enemy r2){
 float distanceX = (r1.x + r1.w/2) - (r2.x + r2.w/2);
 float distanceY = (r1.y + r1.h/2) - (r2.y + r2.h/2);
 
 float combinedHalfW = r1.w/2 + r2.w/2;
 }
 */